{ lib, ... }:

{
  # options.${lib.name}.keympas.bufferline = {
  #   enable = lib.mkEnableOption "${lib.name} keymaps for bufferline";
  #   closeBufferCommand = lib.mkOption {
  #     type = lib.types.string;
  #     default = "bdelete";
  #     description = "Command to close buffer";
  #   };
  # };
}
